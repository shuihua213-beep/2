diff --git a/package.json b/package.json
index 4cd2d7f..ad3aa6c 100644
--- a/package.json
+++ b/package.json
@@ -10,6 +10,7 @@
   },
   "dependencies": {
     "@aidp/web-code-check": "^1.1.10",
+    "immer": "^10.0.0",
     "playwright": "^1.58.2",
     "react": "18.2.0",
     "react-dom": "18.2.0"
diff --git a/src/App.tsx b/src/App.tsx
index 22e120a..85f9c3e 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -1,4 +1,6 @@
 import { useState, useEffect, useCallback, useRef } from 'react';
+import { useHistoryReducer } from './historyReducer';
+import { useUndoRedo } from './useUndoRedo';
 
 // 游戏常量
 const GAME_WIDTH = 900;
@@ -81,24 +83,12 @@ export default function App() {
   const animationRef = useRef<number>(0);
   const lastTimeRef = useRef<number>(0);
   
-  const [gameState, setGameState] = useState<GameState>({
-    status: 'menu',
-    score: 0,
-    coins: 0,
-    artifacts: 0,
-    totalCoins: 0,
-    health: 100,
-    maxHealth: 100,
-    level: 1,
-    distance: 0,
-  });
-
-  const [upgrades, setUpgrades] = useState<Upgrades>({
-    weaponDamage: 0,
-    weaponFireRate: 0,
-    camelHealth: 0,
-    camelSpeed: 0,
-  });
+  // 使用新的 history reducer
+  const { state, dispatch } = useHistoryReducer();
+  const { canUndo, canRedo, undo, redo } = useUndoRedo({ state, dispatch });
+  
+  const gameState = state.present.gameState;
+  const upgrades = state.present.upgrades;
 
   const [enemies, setEnemies] = useState<Enemy[]>([]);
   const [collectibles, setCollectibles] = useState<Collectible[]>([]);
@@ -248,11 +238,20 @@ export default function App() {
     setBackgroundOffset(prev => (prev + speed) % 600);
     setCamelFrame(prev => (prev + 0.15) % 4);
     
-    // 更新距离和等级
-    setGameState(prev => {
-      const newDistance = prev.distance + speed * 0.1;
-      const newLevel = Math.min(5, Math.floor(newDistance / 500) + 1);
-      return { ...prev, distance: newDistance, level: newLevel };
+    // 更新距离和等级 (不记录到历史，因为这是游戏自动更新)
+    dispatch({
+      type: 'LOAD',
+      payload: {
+        ...state,
+        present: {
+          ...state.present,
+          gameState: {
+            ...state.present.gameState,
+            distance: state.present.gameState.distance + speed * 0.1,
+            level: Math.min(5, Math.floor((state.present.gameState.distance + speed * 0.1) / 500) + 1),
+          },
+        },
+      },
     });
     
     // 生成敌人
@@ -298,10 +297,14 @@ export default function App() {
       });
       
       if (damage > 0) {
-        setGameState(prev => ({
-          ...prev,
-          health: Math.max(0, prev.health - damage),
-        }));
+        dispatch({
+          type: 'UPDATE',
+          payload: {
+            gameState: {
+              health: Math.max(0, gameState.health - damage),
+            },
+          },
+        });
       }
       
       return updated;
@@ -341,12 +344,16 @@ export default function App() {
           if (enemyHealth <= 0) {
             // 敌人死亡
             killedEnemyIds.push(enemy.id);
-            setGameState(prev => ({
-              ...prev,
-              score: prev.score + (enemy.type === 'motorcycle' ? 50 : 30),
-              coins: prev.coins + (enemy.type === 'motorcycle' ? 15 : 10),
-              totalCoins: prev.totalCoins + (enemy.type === 'motorcycle' ? 15 : 10),
-            }));
+            dispatch({
+              type: 'UPDATE',
+              payload: {
+                gameState: {
+                  score: gameState.score + (enemy.type === 'motorcycle' ? 50 : 30),
+                  coins: gameState.coins + (enemy.type === 'motorcycle' ? 15 : 10),
+                  totalCoins: gameState.totalCoins + (enemy.type === 'motorcycle' ? 15 : 10),
+                },
+              },
+            });
             createParticles(enemy.x, enemy.y - 20, enemy.type === 'motorcycle' ? '#FF6B6B' : '#8B4513', 15);
           } else if (wasHit) {
             remainingEnemies.push({ ...enemy, health: enemyHealth });
@@ -385,20 +392,25 @@ export default function App() {
           // 收集到物品
           createParticles(c.x, c.y, c.type === 'coin' ? '#FFD700' : '#9333EA', 8);
           
-          setGameState(prevState => {
-            switch (c.type) {
-              case 'coin':
-                return { ...prevState, coins: prevState.coins + 5, score: prevState.score + 5, totalCoins: prevState.totalCoins + 5 };
-              case 'artifact':
-                return { ...prevState, artifacts: prevState.artifacts + 1, score: prevState.score + 100 };
-              case 'shield':
-                return { ...prevState, health: Math.min(prevState.maxHealth, prevState.health + 30) };
-              case 'rapidFire':
-              case 'magnet':
-                return { ...prevState, score: prevState.score + 50 };
-              default:
-                return prevState;
-            }
+          dispatch({
+            type: 'UPDATE',
+            payload: {
+              gameState: (() => {
+                switch (c.type) {
+                  case 'coin':
+                    return { coins: gameState.coins + 5, score: gameState.score + 5, totalCoins: gameState.totalCoins + 5 };
+                  case 'artifact':
+                    return { artifacts: gameState.artifacts + 1, score: gameState.score + 100 };
+                  case 'shield':
+                    return { health: Math.min(gameState.maxHealth, gameState.health + 30) };
+                  case 'rapidFire':
+                  case 'magnet':
+                    return { score: gameState.score + 50 };
+                  default:
+                    return {};
+                }
+              })(),
+            },
           });
         } else {
           remaining.push(c);
@@ -410,7 +422,12 @@ export default function App() {
     
     // 检查游戏结束
     if (gameStateRef.current.health <= 0) {
-      setGameState(prev => ({ ...prev, status: 'gameOver' }));
+      dispatch({
+        type: 'UPDATE',
+        payload: {
+          gameState: { status: 'gameOver' },
+        },
+      });
       return;
     }
     
@@ -433,16 +450,21 @@ export default function App() {
 
   // 开始游戏
   const startGame = () => {
-    setGameState({
-      status: 'playing',
-      score: 0,
-      coins: gameState.coins,
-      artifacts: 0,
-      totalCoins: gameState.coins,
-      health: getMaxHealth(),
-      maxHealth: getMaxHealth(),
-      level: 1,
-      distance: 0,
+    dispatch({
+      type: 'UPDATE',
+      payload: {
+        gameState: {
+          status: 'playing',
+          score: 0,
+          coins: gameState.coins,
+          artifacts: 0,
+          totalCoins: gameState.coins,
+          health: getMaxHealth(),
+          maxHealth: getMaxHealth(),
+          level: 1,
+          distance: 0,
+        },
+      },
     });
     setEnemies([]);
     setCollectibles([]);
@@ -454,11 +476,15 @@ export default function App() {
 
   // 返回主菜单
   const goToMenu = () => {
-    setGameState(prev => ({
-      ...prev,
-      status: 'menu',
-      coins: prev.totalCoins,
-    }));
+    dispatch({
+      type: 'UPDATE',
+      payload: {
+        gameState: {
+          status: 'menu',
+          coins: gameState.totalCoins,
+        },
+      },
+    });
     setShowUpgradePanel(false);
   };
 
@@ -470,20 +496,48 @@ export default function App() {
     const cost = UPGRADE_COSTS[type][level];
     if (gameState.coins < cost) return;
     
-    setGameState(prev => ({
-      ...prev,
-      coins: prev.coins - cost,
-      totalCoins: prev.totalCoins - cost,
-    }));
-    
-    setUpgrades(prev => ({
-      ...prev,
-      [type]: prev[type] + 1,
-    }));
+    dispatch({
+      type: 'UPDATE',
+      payload: {
+        gameState: {
+          coins: gameState.coins - cost,
+          totalCoins: gameState.totalCoins - cost,
+        },
+        upgrades: {
+          [type]: upgrades[type] + 1,
+        },
+      },
+    });
   };
 
   return (
     <div className="min-h-screen bg-gradient-to-b from-amber-200 via-orange-300 to-amber-500 flex flex-col items-center justify-center p-4">
+      {/* 撤销/重做按钮 */}
+      <div className="flex gap-2 mb-4">
+        <button
+          className={`px-4 py-2 rounded-lg font-bold transition-all ${
+            canUndo
+              ? 'bg-blue-500 hover:bg-blue-400 text-white hover:scale-105 shadow-lg'
+              : 'bg-gray-500 text-gray-300 cursor-not-allowed'
+          }`}
+          onClick={undo}
+          disabled={!canUndo}
+        >
+          ↩️ 撤销
+        </button>
+        <button
+          className={`px-4 py-2 rounded-lg font-bold transition-all ${
+            canRedo
+              ? 'bg-purple-500 hover:bg-purple-400 text-white hover:scale-105 shadow-lg'
+              : 'bg-gray-500 text-gray-300 cursor-not-allowed'
+          }`}
+          onClick={redo}
+          disabled={!canRedo}
+        >
+          ↪️ 重做
+        </button>
+      </div>
+      
       <h1 className="text-2xl md:text-4xl font-bold text-amber-900 mb-4 drop-shadow-lg text-center">
         🏜️ 沙丘游侠：宝藏猎手 🐪
       </h1>
